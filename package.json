{
  "name": "mwesh-pychat-app",
  "version": "1.0.0",
  "description": "Mwesh PyChat native app wrapper",
  "scripts": {
    "build:app": "npx cap sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
