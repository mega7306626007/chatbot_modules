# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
venv/

# Runtime artifacts / local state (never commit)
chatbot.db*
generated_images/
model_cache/
story_corpus/

# Downloaded book corpus texts (regenerable via train_story_nn.py)
data/

# Local config - may contain keys; Render gets fresh copies or runs without them
chatbot_llm_config.json
chatbot_translation_config.json

# Editor / OS
.vscode/
.DS_Store
Thumbs.db